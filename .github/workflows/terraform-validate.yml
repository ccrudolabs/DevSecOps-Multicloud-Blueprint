name: Terraform Validate
on: [push]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Terraform Init & Validate
        run: |
          terraform init
          terraform validate